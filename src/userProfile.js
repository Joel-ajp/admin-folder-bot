class UserProfile {
    constructor(parentProject, folderInput) {
        this.parentProject = parentProject;
        this.folderInput = folderInput;
    }
} 

module.exports.UserProfile = UserProfile;